# FitPace
FitPace is a backend API service that tracks a user's fitness goal progress, computes a live trajectory toward that goal, manages a goal state machine, and exposes a conversational /chat endpoint that lets users ask natural language questions about their own progress data.
